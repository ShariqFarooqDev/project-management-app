// server/routes/boardRoutes.js

const express = require('express');
const router = express.Router();

// Placeholder board route
router.get('/', (req, res) => {
  res.send('Board route is working!');
});

module.exports = router;
